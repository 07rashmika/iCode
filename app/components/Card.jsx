import React from 'react'

const Card = ({icon, span, heading, text, buttonLabel, buttonOnClick, buttonType, buttonVariant, buttonSize, buttonDisabled, buttonLink}) => {
  return (
    <div>Card</div>
  )
}

export default Card